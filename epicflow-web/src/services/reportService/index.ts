import http from '../../apiManager/http-request-handler'
const fetchReportData = (reportType: string, reportParams: any) => {
    http.PostRequest('')
}
const ReportService = {
    fetchReportData
}
export default ReportService;