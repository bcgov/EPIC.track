import * as React from 'react';
import { Chip } from '@mui/material';
import { styled } from '@mui/styles';

const EFChip = styled(Chip)({
  backgroundColor: 'rgb(213, 4, 4,0.1)',
  color: 'rgb(213, 4, 4)',
  borderRadius: '4px',
  fontSize: '12px'
})

export default EFChip;