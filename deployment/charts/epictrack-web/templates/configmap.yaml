apiVersion: v1
kind: ConfigMap
metadata:
  labels:
    app: {{ .Chart.Name }}
  name: {{ .Chart.Name }}
data:
  config.js: |-
    // runtime-config.js vars
      window["_env_"] =  {
        "ENV": "dev",
        "NODE_ENV": "production",
        "VITE_API_URL": "{{ .Values.app.api}}",
        "VITE_KEYCLOAK_URL": "{{ .Values.keycloak.url }}",
        "VITE_KEYCLOAK_CLIENT": "{{ .Values.keycloak.client }}",
        "VITE_KEYCLOAK_REALM": "{{ .Values.keycloak.realm }}",
        "VITE_APP_ENV": "{{ .Values.app.env }}",
        "VITE_CENTRE_API_URL": "{{ .Values.app.centreApiUrl }}",
      }