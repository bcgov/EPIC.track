export type PieChartData = {
  name: string;
  value: number;
};

export type WorkByType = {
  count: number;
  work_type: string;
  work_type_id: number;
};

export type AssessmentByPhase = {
  count: number;
  phase: string;
  phase_id: number;
};
